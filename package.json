{
  "name": "codenest",
  "private": true,
  "version": "1.0.0",
  "description": "CodeNest - AI-Powered Collaborative Python Learning Environment",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n srv,web -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "test:keys": "node scripts/test-keys.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
